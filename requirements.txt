# MDMA - Music Design Made Accessible
#
# The authoritative dependency source is pyproject.toml. This file
# mirrors the core + each extra so `pip install -r requirements.txt`
# works without a project checkout. Uncomment the groups you need,
# or run `python scripts/setup_env.py` for an interactive installer.

# Core (required)
numpy>=1.20.0
scipy>=1.7.0
soundfile>=0.10.0
sounddevice>=0.4.0
pydub>=0.25.0

# V2 SignalFlow-native backend (Pattern / Scheduler / PAR /
# InputController). Required for /patn and everything in
# mdma_rebuild/backend/.
# signalflow>=0.5.0
# mido>=1.3.0
# python-rtmidi>=1.5.0
# python-osc>=1.8.0

# Optional: GUI
# wxPython>=4.2.0

# Optional: TUI
# textual>=0.40.0

# Optional: Playback alternatives
# simpleaudio>=1.0.4
# PyAudio>=0.2.11

# Optional: AI features (audio generation, spectral analysis)
# torch>=2.0.0
# transformers>=4.30.0
# diffusers>=0.20.0
# librosa>=0.10.0

# Optional: Stem separation
# demucs>=4.0.0
# spleeter>=2.3.0

# Optional: Streaming (YouTube / SoundCloud ingestion)
# yt-dlp>=2023.0.0
